[package]
name = "summoning-circle"
description = "Summon your processes and ensure they can never die"
edition = "2024"
license = "MIT"
readme = "README.md"
repository = "https://github.com/ncipollo/summoning-circle"
version = "0.1.0"

[dependencies]
anyhow = "=1.0.104"
clap = { version = "=4.6.6", features = ["derive"] }
dirs = "=6.0.0"

[dev-dependencies]
